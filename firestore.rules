rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read and write access to the 'videos' collection
    match /videos/{videoId} {
      allow read, write: if true;
    }
  }
}
