{
  "firestore": {
    "version": 1,
    "collections": {
      "users": {
        "path": "users/{userId}",
        "description": "Stores all user-specific data, including profile and video information. Only the authenticated user can read or write their own documents and subcollections.",
        "rules": "request.auth != null && request.auth.uid == userId"
      }
    }
  },
  "authentication": {
    "signInProviders": {
      "google.com": {
        "enabled": true
      }
    }
  }
}
